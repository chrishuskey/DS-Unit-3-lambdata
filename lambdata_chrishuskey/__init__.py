"""lambdata: A collection of data science helper functions by chrishuskey."""

import numpy as np  # already installed numpy in this project's virtual environment
import pandas as pd  # already installed pandas in this project's virtual environment

# Sample/filler datasets as a placeholder for now:
ZEROS = pd.DataFrame(np.zeros(50))
ONES = pd.DataFrame(np.ones(10))

# Sample/filler functions as a placeholder for now:
def increment(number):
    return (number + 1)
